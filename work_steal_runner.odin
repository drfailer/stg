package stg

// TODO: here, the groups will have 2 queue (standard and shared), and the runner will do batch dequeue and save the elements to their own queue
// - batch dequeue (use odin's default queue container), the queue can pop back as well and we will use this for the thieves

